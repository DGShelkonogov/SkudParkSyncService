namespace SkudParkSyncService.Models
{
    public enum DBConnectionStatus
    {
        OPEN,
        MISSING,
        NOT_CONFIGURATED
    }

    public enum ServerType
    {
        LOCAL,
        REMOTE
    }
}
